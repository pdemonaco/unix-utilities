name: 15 - Conventional Commit Lint

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize

jobs:
  commitlint:
    name: Commit Lint
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Lint commits with commitlint
        uses: wagoid/commitlint-github-action@v5
